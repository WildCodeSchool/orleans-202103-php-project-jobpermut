{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('admin') }}
{% endblock %}

{% block title %}Edit Testimony
{% endblock %}

{% block body %}
    <div class="page-title">
        <h1 class="h1 text-center py-1">Edit Testimony</h1>
    </div>
<div class="container">
<div class="">
    {{ include('testimony/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('testimony_index') }}" class="btn btn-primary">back to list</a>
</div>
    {{ include('testimony/_delete_form.html.twig') }}
    </div>
{% endblock %}

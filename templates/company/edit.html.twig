{% extends 'layout.html.twig' %}

{% block title %}Gestion d'entreprise
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('admin') }}
{% endblock %}

{% block body %}
    <div class="page-title">
        <h1 class="h1 text-center py-1">Éditer une entreprise</h1>
    </div>
    <div class="container">
        <div class="col-2">
            {{ include('company/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            {{ include('company/_delete_form.html.twig') }}
        </div>
        <a href="{{ path('company_index') }}" class="btn btn-primary">Retourner à la liste</a>
    </div>
{% endblock %}
